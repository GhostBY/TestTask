
@model List<UI.Models.ViewModels.CompanyViewModel>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/bootstrap-theme.css" rel="stylesheet" />
    @*<link href="~/Content/ErrorStyles.css" rel="stylesheet" />*@
    <title>Companies</title>
</head>
<body>
    @*<div>
        @if (TempData["message"] != null)
        {
            <div class="alert alert-success">@TempData["message"]</div>
        }
    </div>
    <div id="navbar" class="collapse navbar-collapse" style="background-color:black">
        <ul class="nav navbar-nav">
            <li class="active">@Html.ActionLink("Аккумуляторы", "Accumulators", "Admin")</li>
            <li>@Html.ActionLink("Масла", "Oils", "Admin")</li>
            <li>@Html.ActionLink("Новости", "News", "Admin")</li>
            <li>@Html.ActionLink("Лидеры продаж", "LeaderSells", "Admin")</li>
        </ul>
    </div>*@
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3>Список компаний</h3>
        </div>
        <div class="panel-body">
            <table class="table table-striped table-condensed table-bordered">
                <tr>
                    <th class="text-right">ID</th>
                    <th>Название</th>
                    <th class="text-right">Размер </th>
                    <th class="text-right">Оргпнизационно-правовая форма</th>
                </tr>
                @foreach (var item in Model)
            {
                    <tr>
                        <td class="text-right">@item.Id</td>
                        @*<td>@Html.ActionLink(item.Name, "AccumulatorEdit", new { item.AccumulatorID })</td>*@
                        <td class="text-right">@item.Name</td>
                        <td class="text-right">@item.Size </td>
                        <td class="text-right">@item.OrganizationalForm</td>
                        
                        @*<td class="text-center">
                            @using (Html.BeginForm("DeleteAcc", "Admin"))
                            {
                                @Html.Hidden("AccumulatorID", item.AccumulatorID)
                                <input type="submit"
                                       class="btn btn-default btn-xs"
                                       value="Удалить" />
                            }
                        </td>*@
                    </tr>
                }
            </table>
        </div>
        @*<div class="panel-footer">
            @Html.ActionLink("Добавить аккумулятор", "CreateAcc", null,
            new { @class = "btn btn-default" })
        </div>*@
    </div>
</body>
</html>


